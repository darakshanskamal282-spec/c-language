#include <stdio.h>
int main()
{
    // int a[5]={1,2,3,4,5};
    // printf("first=%d",a[0]);
    // printf("size of a=%d",sizeof(a));    return 0;
    int a[5],i;
    for(i=0;i<=4;i++)
    {
        scanf("%d",&a[i]);
    printf("%d",a[i]+1);       }
    return 0;
}

